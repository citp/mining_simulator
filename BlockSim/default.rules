DefaultCompliant:

Which Block {
    if(exists(ownBlock(h))) {
        extend = ownBlock(m, h)
    } else {
        extend = firstHeard(h)
    }
}

How much {
    value = remaining(B^*
    T ∗(m) = R∗(extend)
Publish?:
    P∗(m) ∋ own-Bim, for all i ≥ h∗fdssdf if they exist.


Symbols

B^*(m) = Own-B^m_(h^*)